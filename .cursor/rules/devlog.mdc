---
alwaysApply: true
---

开发日志维护

本项目使用 `docs/DEVLOG.md` 记录开发过程中的问题、原因、解决方案与优化。

在以下情况完成后，**必须**在 `docs/DEVLOG.md` 的「日志条目」章节**顶部**追加一条新记录（最新在上）：

1. 修复 Bug 或排查并确认根因
2. 完成功能优化或配置改进
3. 发现重要踩坑或设计限制（即使用户未要求文档）
4. 完成 Spec 实现中值得留档的决策或权衡

每条记录应包含：

- 日期（YYYY-MM-DD）与简短标题
- **类型：** Bug / 优化 / 踩坑 / 设计说明 / 配置
- **状态：** 已解决 / 进行中 / 待处理 / 已知限制
- **影响范围：** 相关模块或文件
- **现象、原因、解决/做法**（无代码变更时写「暂无」或操作步骤）
- 可选 **备注**（Spec ID、后续 TODO）

格式模板见 `docs/DEVLOG.md` 顶部「条目格式」。

规则：

- 只追加与本次工作相关的条目，不重写整份日志
- 若问题已在 DEVLOG 中且本次仅推进状态，可更新原条目状态而非重复新建
- 待办项变化时同步更新 DEVLOG 底部「待办汇总」表
- 不要记录密钥、webhook 完整 URL 等敏感信息（可脱敏如 `****d5f6`）
